import { View } from 'react-native';
import Text from '../Text';
import * as yup from 'yup';

const ReviewForm = ({ onSubmit, initialValues }) => {
  return (
    <View>
      <Text>Review Form Goes here</Text>
    </View>
  );
};

export default ReviewForm;
